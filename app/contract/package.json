{
  "name": "culture-drops-contracts",
  "version": "1.0.0",
  "description": "Culture Drops NFT smart contracts for Base blockchain",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:base-sepolia": "hardhat run scripts/deploy.js --network base-sepolia",
    "deploy:base": "hardhat run scripts/deploy.js --network base",
    "verify:base-sepolia": "hardhat verify --network base-sepolia",
    "verify:base": "hardhat verify --network base"
  },
  "keywords": ["nft", "culture", "base", "blockchain", "solidity"],
  "author": "Culture Drops Team",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.3.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
